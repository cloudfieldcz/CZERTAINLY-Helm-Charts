CHART NAME: {{ .Chart.Name }}
CHART VERSION: {{ .Chart.Version }}
APP VERSION: {{ .Chart.AppVersion }}

** Please be patient while the chart is being deployed **

CZERTAINLY Redis Caching Service has been installed.

Redis is running at: {{ include "caching-redis.fullname" . }}:{{ .Values.service.port }}

To verify the installation:
  kubectl exec -it deployment/{{ include "caching-redis.fullname" . }} -- redis-cli ping

{{- if pluck "password" .Values.global.redis .Values.redis | compact | first }}

Note: Redis is password-protected. Use -a flag with redis-cli:
  kubectl exec -it deployment/{{ include "caching-redis.fullname" . }} -- redis-cli -a "$REDIS_PASSWORD" ping
{{- end }}

For more information, visit: https://docs.czertainly.com
